services:
  stirling-pdf:
    image: frooodle/s-pdf:latest
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./.docker/configs:/configs
      - ./.docker/logs:/logs
    environment:
      DOCKER_ENABLE_SECURITY: "false"
