# MCPHub dashboard admin password (username is always `admin`).
# Sandbox-safe default — change before any non-local use.
# If unset, MCPHub generates a random password and prints it to the logs.
ADMIN_PASSWORD=admin
